# Native iOS app

The SwiftUI app is now in its own repository:

**https://github.com/GreatTA1998/ios.actions.life**

```bash
cd ~/Developer
git clone git@github.com:GreatTA1998/ios.actions.life.git
open ios.actions.life/ActionsLife.xcodeproj
```

This `ios/` tree is leftover from the first-slice PR. Capacitor/`App` is unrelated. Do not treat `ActionsLife.xcodeproj` here as the source of truth.
