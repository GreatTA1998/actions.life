// needed for cmd + click (jump to definition) in the IDE
{
  "compilerOptions": {
    "paths": {
      "$lib/*": ["./src/lib/*"]
    }
  },
  "include": ["src/**/*"],
  "exclude": [
    "node_modules",
    "functions/node_modules",
    "static"
  ]
}